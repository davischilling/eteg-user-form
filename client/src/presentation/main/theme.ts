import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: { main: '#f5f5f1' },
    secondary: { main: '#55A624' },
    text: { primary: '#f5f5f1' },
  },
});
